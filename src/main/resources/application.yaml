spring:
  application:
    name: SyllabusAI

  datasource:
    url: jdbc:postgresql://localhost:5432/syllabusai
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
